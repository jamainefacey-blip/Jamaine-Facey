# Environment variables — never commit
.env
.env.*
.env.local
.env.production

node_modules

*/.DS_Store

.vscode

# Platform monorepo (tracked on vst-platform-core branch, not this one)
vst-platform/