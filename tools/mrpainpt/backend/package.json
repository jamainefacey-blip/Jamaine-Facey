{
  "name": "mrpainpt-backend",
  "version": "1.0.0",
  "description": "Mr Pain PT — Client + Access Management API",
  "main": "server.js",
  "scripts": {
    "start":          "node server.js",
    "dev":            "node --watch server.js",
    "seed":           "node seed.js",
    "seed:overwrite": "node seed.js --overwrite",
    "seed:dry":       "node seed.js --dry-run"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "dotenv": "17.3.1",
    "express": "^4.18.3"
  }
}
